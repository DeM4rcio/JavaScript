const num = 10;// number

const nome = "Márcio";//String

let nomeAluno; // Undefined -> não aponta pra nenhum local da memória

let cor = null // Nulo -> não aponta pra nenhum local da memória

const aprovado = true; // Boolean = true, fasle (lógico)

console.log(typeof cor); // podemos verificar o tipo de dado com a função "typeof"

